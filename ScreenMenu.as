package {	import flash.display.MovieClip;	import flash.events.Event;	import flash.events.MouseEvent;	import flash.media.SoundChannel;	public class ScreenMenu extends MovieClip	{		private var _soundChannel:SoundChannel;		private var _bgMusic:menuscreen;				public function ScreenMenu()		{			addEventListener(Event.ADDED_TO_STAGE, onAddedToStage);		}		private function onAddedToStage(e:Event):void		{			// set variables			_soundChannel = new SoundChannel();			_bgMusic = new menuscreen();						_soundChannel = _bgMusic.play(0,int.MAX_VALUE);			// add listeners			addEventListener(Event.REMOVED_FROM_STAGE, onRemovedFromStage);			start_btn.addEventListener(MouseEvent.CLICK, onStart);			instructions_btn.addEventListener(MouseEvent.CLICK, onInstructions);		}		private function onRemovedFromStage(e:Event):void		{			// remove listeners			removeEventListener(Event.ADDED_TO_STAGE, onAddedToStage);			removeEventListener(Event.REMOVED_FROM_STAGE, onRemovedFromStage);			start_btn.removeEventListener(MouseEvent.CLICK, onStart);			instructions_btn.removeEventListener(MouseEvent.CLICK, onInstructions);		}				private function onStart(e:MouseEvent):void		{			var gameScreen:ScreenGame = new ScreenGame();			MovieClip(parent).addChild(gameScreen);			MovieClip(parent).removeChild(this);		}				private function onInstructions(e:MouseEvent):void		{			var instructionsScreen:ScreenInstructions = new ScreenInstructions();			MovieClip(parent).addChild(instructionsScreen);			MovieClip(parent).removeChild(this);		}	}}